trigger:
  branches:
    include:
      - main
      - feature/*

pool:
  vmImage: 'ubuntu-latest'

steps:
- checkout: self

- script: |
    echo "Repository cloned successfully."
    pwd
    ls -la
  displayName: "Verify Repository Checkout"